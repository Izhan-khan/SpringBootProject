package com.innovento.Repository;

import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

@Repository
public interface UniversityMasterRepo extends JpaRepository<UniversityMasterRepo, Long> {
	
	public UniversityMasterRepo findByuId(long uId);

}
